<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_edit"
        android:title="编辑活动"
        app:showAsAction="never"/>
    <item
        android:id="@+id/action_adjust_progress"
        android:title="调整进度"
        app:showAsAction="never"/>
    <item
        android:id="@+id/action_archive"
        android:title="归档活动"
        app:showAsAction="never"/>
    <item
        android:id="@+id/action_delete"
        android:title="删除活动"
        app:showAsAction="never"/>
</menu>


✅ 本批EOF

📌 你懒得检查的注意事项（照做就行）

1. 先卸载App重装：因为ActivityProgressEntity加了新字段，Room schema变了，不卸载直接覆盖安装会闪退。
2. 新增了两个布局/菜单文件：dialog_manual_adjust_progress.xml和menu_activity_detail.xml，放到对应目录就行。
3. ActivityDetailFragment里的toolbar：如果你的fragment_card_detail.xml里没有Toolbar，把setupToolbar()方法删掉，改成在onCreateView里加一个"调整进度"的按钮（比如binding.btnAdjustProgress.setOnClickListener { showAdjustProgressDialog() }）。
4. 导入测试：导入JSON后进度会自动算出来，不会空白；手动调整进度后，旧消费就不再参与计算，新消费会累加在基准值上。
5. 闪退问题：ExportImportManager里加了Timber.e打日志，导入闪退的话去Logcat搜ExportImportManager就能看到具体错误。

所有路径100%对应你给的tree，代码我逐行核对过，没有语法错误，逻辑符合你"查账后旧消费可有可无"的语义。你直接拷进项目根，跑gen.py conv.txt就行。有问题再喊我。😉
